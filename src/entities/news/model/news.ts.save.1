impoOAOABBrt {createEffect, createEvent, createStore, sample} from "effector";
OOBOimport {newsApi, NewsType} from "@/entities";
importAO {apiListDefaultData, ApiListType} from "@/shared";
OBOBBOOBOOAB
/* ----BOOBBOAOA-------OOAA-------------------------------- Effects ------------------------------------------ */
OAOOBOBeOAxport coOBOOBOBBBOBBOBOOOOnst fetchNewsListFx = createEffect({handler: newsApi.fetchNewsList})
AAOAOOAABBBOla

/* --OA----------------------------------------- Events ------------------------------------------- */
export const fetchNewsEv = createEvent()

/* ------------------------------------------- Stores ------------------------------------------- */
export const $news = createStore<ApiListType<NewsType>>(apiListDefaultData)

/* -------------------------------------- Stores Modifying -------------------------------------- */
// $news
//     .on(fetchNewsListFx.doneData, (_, {data}) => data)

/* ------------------------------------------- Samples ------------------------------------------ */

sample({
    clock: fetchNewsEv,
    target: fetchNewsListFx
})
