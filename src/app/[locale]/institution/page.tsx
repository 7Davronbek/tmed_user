import {InstitutionList} from "@/features/institution";

const InstitutionPage = async () => {
    return (
        <div className={'container'}>
            <InstitutionList/>
        </div>
    )
};

export default InstitutionPage;