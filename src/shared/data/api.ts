import {ApiListType} from "@/shared";

export const apiListDefaultData: ApiListType<any> = {
    count: 0,
    next: null,
    previous: null,
    next_offset: 0,
    previous_offset: 0,
    results: []
}
